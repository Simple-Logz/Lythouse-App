import{createClient}from"jsr:@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":Deno.env.get("APP_ORIGIN")||"https://sandbox-ai-app-eight.vercel.app","Access-Control-Allow-Methods":"POST, OPTIONS","Access-Control-Allow-Headers":"Content-Type, Authorization, X-Client-Info, Apikey"};
type Sev='low'|'medium'|'high'|'critical';type Finding={category:string;severity:Sev;title:string;description:string;file_path:string|null;line:number|null;recommendation:string|null;confidence:number|null};
const SKIP=['node_modules','.git','dist','build','.next','vendor','.venv','coverage'];const MAX_FILES=350,MAX_SIZE=180000;
const secretRules=[['AWS access key',/AKIA[0-9A-Z]{16}/g,'critical'],['GitHub token',/(?:ghp_[A-Za-z0-9]{36}|github_pat_[A-Za-z0-9_]{22,})/g,'critical'],['Stripe live secret',/sk_live_[0-9A-Za-z]{24,}/g,'critical'],['Private key',/-----BEGIN (?:RSA |EC |DSA |OPENSSH )?PRIVATE KEY-----/g,'critical'],['Database URL with credentials',/postgres(?:ql)?:\/\/[^:\s]+:[^@\s]+@[^\s]+/gi,'high'],['Hardcoded secret',/(?:password|secret|token|api_key|apikey)\s*[:=]\s*["'][^"'\s]{16,}["']/gi,'high']] as const;
const staticRules=[['Dangerous eval',/\beval\s*\(/g,'high','static_analysis'],['Unsafe HTML injection',/dangerouslySetInnerHTML|\.innerHTML\s*=/g,'high','static_analysis'],['Command execution',/\bexec\s*\(/g,'high','static_analysis'],['TLS verification disabled',/rejectUnauthorized\s*:\s*false|verify\s*:\s*false/gi,'high','configuration'],['Wildcard CORS',/Access-Control-Allow-Origin["']?\s*[:=]\s*["']\*["']/g,'medium','configuration'],['SQL interpolation',/(?:query|execute|raw)\s*\(\s*["'`].*\$\{/gi,'high','static_analysis']] as const;
function lineOf(s:string,i:number){return s.slice(0,i).split('\n').length}function finding(category:string,severity:Sev,title:string,path:string,line:number,rec:string,confidence=85):Finding{return{category,severity,title,description:`${title} detected in ${path}:${line}.`,file_path:path,line,recommendation:rec,confidence}}
function scanGeneric(c:string,p:string){const out:Finding[]=[];for(const[n,r,s]of secretRules){r.lastIndex=0;let m;while((m=r.exec(c)))out.push(finding('secret_scan',s,n,p,lineOf(c,m.index),'Remove and rotate the credential; use a managed secret or environment variable.',95))}for(const[n,r,s,cat]of staticRules){r.lastIndex=0;let m;while((m=r.exec(c)))out.push(finding(cat,s,n,p,lineOf(c,m.index),'Replace this pattern with a safer, least-privilege implementation.'))}return out}
function scanInfra(c:string,p:string){const o:Finding[]=[];const add=(sev:Sev,t:string,rec:string,idx=0)=>o.push(finding('infrastructure',sev,t,p,lineOf(c,Math.max(0,idx)),rec,88));if(/Dockerfile$/i.test(p)){if(/FROM\s+[^\n:]+:latest/i.test(c))add('medium','Unpinned Docker base image','Pin an immutable version or digest.');if(!/^USER\s+/mi.test(c))add('medium','Container may run as root','Set a non-root USER in the final image.')}if(/\.github\/workflows\/.*\.ya?ml$/i.test(p)){if(/permissions:\s*write-all/i.test(c))add('high','GitHub Actions write-all permissions','Grant only required workflow permissions.');if(/uses:\s*[^\s]+@(main|master|latest)/i.test(c))add('medium','Unpinned GitHub Action','Pin actions to a commit SHA.')}if(/\.tf$/i.test(p)&&/0\.0\.0\.0\/0/i.test(c))add('high','Terraform exposes resource to the internet','Restrict ingress CIDRs.');return o}
function priority(p:string){if(/\.env|secret|credential/i.test(p))return 100;if(/package\.json/i.test(p))return 95;if(/\.github\/workflows|Dockerfile|\.tf$/i.test(p))return 90;if(/auth|security|migration/i.test(p))return 80;return 20}function eligible(p:string){return!p.split('/').some(x=>SKIP.includes(x))&&/\.(js|ts|jsx|tsx|py|go|rb|rs|java|kt|php|sh|ya?ml|json|toml|tf|env|config)$|Dockerfile$|requirements\.txt$|go\.mod$/i.test(p)}
function parseGit(u:string){const m=u.match(/github\.com[/:]([^/]+)\/(.+?)(?:\.git)?$/);return m?{owner:m[1],repo:m[2]}:null}async function gh(url:string,token:string|null,raw=false){const h:Record<string,string>={Accept:raw?'application/vnd.github.raw+json':'application/vnd.github+json','User-Agent':'lythouse'};if(token)h.Authorization=`Bearer ${token}`;const r=await fetch(url,{headers:h});if(!r.ok)return null;return raw?r.text():r.json()}
function score(fs:Finding[]){const w={critical:25,high:15,medium:8,low:3};const n=Math.min(100,fs.reduce((a,f)=>a+w[f.severity],0));const severity:Sev|'none'=fs.some(f=>f.severity==='critical')?'critical':n>=50?'high':n>=25?'medium':n>0?'low':'none';return{n,severity}};
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response(null,{status:204,headers:cors});const j=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...cors,'Content-Type':'application/json'}});let validationId:string|undefined;let db:any;try{const auth=req.headers.get('Authorization')||'';const token=auth.replace(/^Bearer\s+/i,'');if(!token)return j({error:'Authentication required'},401);db=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const{data:{user},error:ue}=await db.auth.getUser(token);if(ue||!user)return j({error:'Invalid session'},401);({validationId}=await req.json());if(!validationId)return j({error:'validationId required'},400);const{data:v,error:ve}=await db.from('validations').select('*').eq('id',validationId).single();if(ve||!v)return j({error:'Validation not found'},404);const{data:member}=await db.from('workspace_members').select('id').eq('workspace_id',v.workspace_id).eq('user_id',user.id).maybeSingle();if(!member)return j({error:'Forbidden'},403);const{data:p,error:pe}=await db.from('projects').select('*').eq('id',v.project_id).eq('workspace_id',v.workspace_id).single();if(pe||!p)return j({error:'Project not found in this workspace'},404);await db.from('validations').update({status:'running'}).eq('id',validationId);const steps=[['repository','Read repository'],['scan','Scan code and infrastructure'],['score','Calculate risk and verdict']];await db.from('validation_steps').delete().eq('validation_id',validationId);await db.from('validation_steps').insert(steps.map((s,i)=>({validation_id:validationId,step_index:i,key:s[0],name:s[1],status:i===0?'running':'pending',started_at:i===0?new Date().toISOString():null})));const parsed=parseGit(p.git_url||'');if(!parsed)throw new Error('Invalid GitHub repository URL');const branch=p.git_branch||'main',ghToken=p.github_token||null;const tree=await gh(`https://api.github.com/repos/${parsed.owner}/${parsed.repo}/git/trees/${encodeURIComponent(branch)}?recursive=1`,ghToken);if(!tree)throw new Error('Unable to read repository');await db.from('validation_steps').update({status:'completed',completed_at:new Date().toISOString()}).eq('validation_id',validationId).eq('step_index',0);await db.from('validation_steps').update({status:'running',started_at:new Date().toISOString()}).eq('validation_id',validationId).eq('step_index',1);const all=(tree.tree||[]).filter((x:any)=>x.type==='blob'&&eligible(x.path)).sort((a:any,b:any)=>priority(b.path)-priority(a.path));const selected=all.slice(0,MAX_FILES);const findings:Finding[]=[];let scanned=0;for(const f of selected){const c=await gh(`https://api.github.com/repos/${parsed.owner}/${parsed.repo}/contents/${encodeURIComponent(f.path).replace(/%2F/g,'/')}?ref=${encodeURIComponent(branch)}`,ghToken,true);if(!c||c.length>MAX_SIZE)continue;scanned++;findings.push(...scanGeneric(c,f.path),...scanInfra(c,f.path));}await db.from('findings').delete().eq('validation_id',validationId);if(findings.length)await db.from('findings').insert(findings.map(f=>({validation_id:validationId,project_id:v.project_id,...f})));await db.from('validation_steps').update({status:'completed',completed_at:new Date().toISOString()}).eq('validation_id',validationId).eq('step_index',1);await db.from('validation_steps').update({status:'running',started_at:new Date().toISOString()}).eq('validation_id',validationId).eq('step_index',2);const rs=score(findings),counts={critical:0,high:0,medium:0,low:0};findings.forEach(f=>counts[f.severity]++);const verdict=rs.severity==='critical'||rs.n>=70?'BLOCK':rs.n>=35?'REVIEW':'SHIP';const coverage=all.length?Math.round(scanned/all.length*100):100;const summary=`${verdict}: scanned ${scanned} prioritized files (${coverage}% of ${all.length} eligible files). Found ${counts.critical} critical, ${counts.high} high, ${counts.medium} medium and ${counts.low} low issues.`;await db.from('validations').update({status:'completed',risk_score:rs.n,severity:rs.severity,summary,total_findings:findings.length,critical_count:counts.critical,high_count:counts.high,medium_count:counts.medium,low_count:counts.low,completed_at:new Date().toISOString()}).eq('id',validationId);await db.from('validation_steps').update({status:'completed',detail:verdict,completed_at:new Date().toISOString()}).eq('validation_id',validationId).eq('step_index',2);const{data:w}=await db.from('workspaces').select('organization_id').eq('id',v.workspace_id).single();await db.from('usage_events').insert({organization_id:w?.organization_id||null,workspace_id:v.workspace_id,user_id:user.id,project_id:v.project_id,event_type:'validation.run',quantity:1,metadata:{validation_id:validationId,risk_score:rs.n,verdict,files_scanned:scanned}});await db.from('audit_events').insert({organization_id:w?.organization_id||null,workspace_id:v.workspace_id,project_id:v.project_id,actor_id:user.id,event_type:'validation.completed',entity_type:'validation',entity_id:validationId,metadata:{risk_score:rs.n,verdict,files_scanned:scanned}});return j({success:true,validationId,filesScanned:scanned,eligibleFiles:all.length,coverage,findings:findings.length,riskScore:rs.n,severity:rs.severity,verdict,summary});}catch(e){console.error(e);if(validationId&&db){try{await db.from('validations').update({status:'failed',summary:e instanceof Error?e.message:'Validation failed'}).eq('id',validationId);await db.from('validation_steps').update({status:'failed',detail:e instanceof Error?e.message:'Validation failed',completed_at:new Date().toISOString()}).eq('validation_id',validationId).eq('status','running')}catch{}}return j({error:e instanceof Error?e.message:'Validation failed'},500)}});
