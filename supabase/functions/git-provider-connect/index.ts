import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':Deno.env.get('APP_ORIGIN')||'*','Access-Control-Allow-Headers':'authorization, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS'};
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors,'Content-Type':'application/json'}});
type Provider='github'|'gitlab'|'bitbucket'|'azure_devops';
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return json({error:'Method not allowed'},405);try{const auth=req.headers.get('Authorization')||'';if(!auth.startsWith('Bearer '))return json({error:'Authentication required'},401);const supabaseUrl=Deno.env.get('SUPABASE_URL')!,anon=Deno.env.get('SUPABASE_ANON_KEY')!,service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;const userClient=createClient(supabaseUrl,anon,{global:{headers:{Authorization:auth}}});const{data:{user},error:ue}=await userClient.auth.getUser();if(ue||!user)return json({error:'Authentication required'},401);const body=await req.json();const workspaceId=String(body.workspaceId||'');const provider=String(body.provider||'') as Provider;if(!workspaceId)return json({error:'workspaceId is required'},400);if(!['github','gitlab','bitbucket','azure_devops'].includes(provider))return json({error:'Unsupported source-control provider'},400);const db=createClient(supabaseUrl,service);const{data:member}=await db.from('workspace_members').select('role').eq('workspace_id',workspaceId).eq('user_id',user.id).maybeSingle();if(!member||!['owner','admin','developer'].includes(member.role))return json({error:'You do not have permission to connect repositories'},403);const origin=Deno.env.get('APP_ORIGIN')||'';const state=encodeURIComponent(btoa(JSON.stringify({workspaceId,userId:user.id,provider,ts:Date.now()})));let authorizeUrl='';if(provider==='github'){const slug=Deno.env.get('GITHUB_APP_SLUG');if(!slug)return json({error:'GitHub App is not configured yet.'},503);authorizeUrl=`https://github.com/apps/${encodeURIComponent(slug)}/installations/new?state=${state}`;}else if(provider==='gitlab'){const id=Deno.env.get('GITLAB_CLIENT_ID');if(!id)return json({error:'GitLab OAuth is not configured yet.'},503);authorizeUrl=`https://gitlab.com/oauth/authorize?client_id=${encodeURIComponent(id)}&redirect_uri=${encodeURIComponent(origin+'/integrations/gitlab/callback')}&response_type=code&state=${state}&scope=${encodeURIComponent('read_api read_repository')}`;}else if(provider==='bitbucket'){const id=Deno.env.get('BITBUCKET_CLIENT_ID');if(!id)return json({error:'Bitbucket authorization is not configured yet.'},503);authorizeUrl=`https://bitbucket.org/site/oauth2/authorize?client_id=${encodeURIComponent(id)}&response_type=code&state=${state}`;}else{const id=Deno.env.get('AZURE_DEVOPS_CLIENT_ID');if(!id)return json({error:'Azure DevOps authorization is not configured yet.'},503);authorizeUrl=`https://app.vssps.visualstudio.com/oauth2/authorize?client_id=${encodeURIComponent(id)}&response_type=Assertion&state=${state}&scope=${encodeURIComponent('vso.code')}&redirect_uri=${encodeURIComponent(origin+'/integrations/azure-devops/callback')}`;}return json({provider,authorizeUrl});}catch(e){console.error('git-provider-connect',e);return json({error:'Repository connection could not be started.'},500);}});
