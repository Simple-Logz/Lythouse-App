import{resolveActiveWorkspace,supabase}from'./supabase';import{requireWorkspaceFeature}from'./planLimits';export type ApiTestResult={targetOrigin:string;status:number;ok:boolean;durationMs:number;tls:boolean;contentType:string;checkedAt:string};export async function executeApiTest(baseUrl:string,timeoutMs=10000){const w=await resolveActiveWorkspace();await requireWorkspaceFeature('api_testing',w.id);const{data,error}=await supabase.functions.invoke('api-test-executor',{body:{workspaceId:w.id,baseUrl,timeoutMs}});if(error)throw error;if(data?.error)throw new Error(data.error);if(!data?.result)throw new Error('API test returned no result');return data.result as ApiTestResult}
