import{describe,expect,it}from'vitest';import{assessExecutionTarget}from'./targetSafety';describe('controlled execution safety',()=>{it('blocks private targets',()=>{expect(assessExecutionTarget('https://10.0.0.2','chaos').allowed).toBe(false)});it('blocks public execution until isolated authorization exists',()=>{expect(assessExecutionTarget('https://example.com','load').allowed).toBe(false)})})
