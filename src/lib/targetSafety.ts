export type ExecutionKind='load'|'chaos';export function assessExecutionTarget(raw:string,kind:ExecutionKind){let u:URL;try{u=new URL(raw)}catch{return{allowed:false,reason:'A valid target URL is required.'}}if(u.protocol!=='https:')return{allowed:false,reason:'Only HTTPS targets are permitted.'};const h=u.hostname.toLowerCase();if(h==='localhost'||h.endsWith('.local')||/^127\.|^10\.|^192\.168\.|^169\.254\.|^172\.(1[6-9]|2\d|3[01])\./.test(h))return{allowed:false,reason:'Private and local network targets are blocked.'};return{allowed:false,reason:`${kind==='chaos'?'Chaos':'Load'} execution requires an isolated authorized executor. The web application cannot execute this operation directly.`}}
