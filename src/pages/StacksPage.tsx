// @ts-nocheck
import {useCallback,useEffect,useState} from'react';import{supabase}from'../lib/supabase';import{EmptyState,Spinner,SeverityBadge}from'../lib/ui';import{Layers,Plus,X,Github,Server,ShieldCheck,GitPullRequest,Rocket,Activity}from'lucide-react';
const tabs=['Overview','Projects','Environments','Validations','Changes','Deployments','Activity','Settings'];
export function StacksPage(){const wsId=localStorage.getItem('sandbox.activeWs')||'';const[loading,setLoading]=useState(true),[error,setError]=useState(''),[stacks,setStacks]=useState([]),[projects,setProjects]=useState([]),[envs,setEnvs]=useState([]),[validations,setValidations]=useState([]),[changes,setChanges]=useState([]),[deployments,setDeployments]=useState([]),[selected,setSelected]=useState(null),[tab,setTab]=useState('Overview'),[creating,setCreating]=useState(false),[name,setName]=useState(''),[description,setDescription]=useState(''),[busy,setBusy]=useState(false);const load=useCallback(async()=>{if(!wsId){setError('No active workspace. Choose or create a workspace first.');setLoading(false);return}setLoading(true);setError('');const[s,p,e,v,c,d]=await Promise.all([supabase.from('stacks').select('*').eq('workspace_id',wsId).order('created_at',{ascending:false}),supabase.from('projects').select('id,stack_id,name,description,status,git_url,git_branch').eq('workspace_id',wsId),supabase.from('environments').select('id,stack_id,project_id,name,kind').eq('workspace_id',wsId),supabase.from('validations').select('id,project_id,status,risk_score,severity,summary,created_at').eq('workspace_id',wsId).order('created_at',{ascending:false}),supabase.from('change_requests').select('id,project_id,title,status,risk_level,created_at').eq('workspace_id',wsId).order('created_at',{ascending:false}),supabase.from('deployments').select('id,project_id,change_request_id,status,provider,created_at,completed_at').eq('workspace_id',wsId).order('created_at',{ascending:false})]);const bad=[s,p,e,v,c,d].find(x=>x.error);if(bad?.error)setError(bad.error.message);setStacks(s.data||[]);setProjects(p.data||[]);setEnvs(e.data||[]);setValidations(v.data||[]);setChanges(c.data||[]);setDeployments(d.data||[]);setLoading(false)},[wsId]);useEffect(()=>{load()},[load]);async function create(){if(!name.trim())return;setBusy(true);setError('');const{data:{user}}=await supabase.auth.getUser();if(!user){setError('Your session expired. Sign in again.');setBusy(false);return}const{data,error}=await supabase.from('stacks').insert({workspace_id:wsId,name:name.trim(),description:description.trim()||null,created_by:user.id}).select('*').single();if(error){setError(error.message);setBusy(false);return}setStacks(x=>[data,...x]);setCreating(false);setName('');setDescription('');setSelected(data);setTab('Overview');setBusy(false)}async function assignProject(id,stackId){const{error}=await supabase.from('projects').update({stack_id:stackId}).eq('id',id);if(error){setError(error.message);return}setProjects(x=>x.map(p=>p.id===id?{...p,stack_id:stackId}:p))}if(loading)return <div className="flex justify-center py-24"><Spinner size={28}/></div>;if(selected){const ps=projects.filter(p=>p.stack_id===selected.id),ids=new Set(ps.map(p=>p.id)),es=envs.filter(e=>e.stack_id===selected.id||ids.has(e.project_id)),vs=validations.filter(v=>ids.has(v.project_id)),cs=changes.filter(c=>ids.has(c.project_id)),ds=deployments.filter(d=>ids.has(d.project_id)),latest=vs[0],critical=vs.filter(v=>v.severity==='critical').length,health=latest?.risk_score==null?null:Math.max(0,100-latest.risk_score);return <div className="max-w-7xl mx-auto"><button className="text-sm text-gray-500 mb-5" onClick={()=>setSelected(null)}>← All stacks</button><div className="flex items-start justify-between"><div className="flex gap-3"><span className="w-11 h-11 rounded-xl bg-violet-50 text-violet-600 grid place-items-center"><Layers size={22}/></span><div><h1 className="text-2xl font-bold">{selected.name}</h1><p className="text-sm text-gray-500 mt-1">{selected.description||'Application system'}</p></div></div><span className="text-xs font-semibold bg-emerald-50 text-emerald-700 rounded-full px-3 py-1.5">{selected.status}</span></div><div className="mt-7 border-b overflow-x-auto"><div className="flex min-w-max gap-7">{tabs.map(t=><button key={t} onClick={()=>setTab(t)} className={`pb-3 text-sm font-medium border-b-2 ${tab===t?'border-violet-600 text-violet-700':'border-transparent text-gray-500'}`}>{t}</button>)}</div></div>{error&&<Err>{error}</Err>}<div className="mt-6">{tab==='Overview'&&<><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-4"><Metric icon={<Github size={18}/>} value={ps.length} label="Projects"/><Metric icon={<Server size={18}/>} value={es.length} label="Environments"/><Metric icon={<ShieldCheck size={18}/>} value={vs.length} label="Validations"/><Metric icon={<Activity size={18}/>} value={health==null?'—':health} label="Readiness"/></div><div className={`mt-5 rounded-xl border p-4 ${critical?'border-red-200 bg-red-50':'border-emerald-200 bg-emerald-50'}`}><p className="font-semibold">{critical?`${critical} critical validation result${critical===1?'':'s'} require attention`:vs.length?'No critical validation results in this stack':'No validations run yet'}</p><p className="text-sm mt-1 opacity-75">A stack is one application or service. Its readiness rolls up evidence from the repositories and environments that make it deployable.</p></div><Section title="Projects in this stack">{ps.length?ps.map(p=><Row key={p.id} icon={<Github size={16}/>} title={p.name} sub={p.git_url}/>):<p className="text-sm text-gray-500">No projects assigned yet. Open Projects above to add repositories.</p>}</Section></>}{tab==='Projects'&&<Section title="Projects"><p className="text-sm text-gray-500 mb-4">Assign workspace repositories to this application or service.</p>{projects.length?projects.map(p=><div key={p.id} className="flex items-center justify-between gap-4 py-3 border-t first:border-t-0"><div><p className="font-medium">{p.name}</p><p className="text-xs text-gray-500">{p.git_url}</p></div><button className={p.stack_id===selected.id?'btn-secondary':'btn-primary'} onClick={()=>assignProject(p.id,p.stack_id===selected.id?null:selected.id)}>{p.stack_id===selected.id?'Remove':'Add to stack'}</button></div>):<p className="text-sm text-gray-500">Connect a project to the workspace first.</p>}</Section>}{tab==='Environments'&&<Section title="Environments">{es.length?es.map(e=><Row key={e.id} icon={<Server size={16}/>} title={e.name} sub={e.kind}/>):<p className="text-sm text-gray-500">No environments connected to this stack.</p>}</Section>}{tab==='Validations'&&<Section title="Validation history">{vs.length?vs.map(v=><div key={v.id} className="flex justify-between py-3 border-t first:border-t-0"><div><p className="font-medium">{v.summary||'Repository validation'}</p><p className="text-xs text-gray-500">{new Date(v.created_at).toLocaleString()}</p></div><div className="flex items-center gap-2"><SeverityBadge severity={v.severity||'none'}/><b>{v.risk_score??'—'}</b></div></div>):<p className="text-sm text-gray-500">No validation evidence yet.</p>}</Section>}{tab==='Changes'&&<Section title="Changes">{cs.length?cs.map(c=><Row key={c.id} icon={<GitPullRequest size={16}/>} title={c.title} sub={`${c.status}${c.risk_level?` · ${c.risk_level} risk`:''}`}/>):<p className="text-sm text-gray-500">No changes recorded for this stack.</p>}</Section>}{tab==='Deployments'&&<Section title="Deployments">{ds.length?ds.map(d=><Row key={d.id} icon={<Rocket size={16}/>} title={d.provider||'Deployment'} sub={`${d.status} · ${new Date(d.created_at).toLocaleString()}`}/>):<p className="text-sm text-gray-500">No deployments recorded for this stack.</p>}</Section>}{tab==='Activity'&&<Section title="Stack activity"><p className="text-sm text-gray-500 mb-3">Recent evidence across validations, changes and deployments.</p>{[...vs.slice(0,3).map(v=>({id:v.id,t:'Validation',s:v.summary||v.status})),...cs.slice(0,3).map(c=>({id:c.id,t:'Change',s:`${c.title} · ${c.status}`})),...ds.slice(0,3).map(d=>({id:d.id,t:'Deployment',s:d.status}))].map(x=><Row key={x.id} icon={<Activity size={16}/>} title={x.t} sub={x.s}/>)}</Section>}{tab==='Settings'&&<Section title="Stack settings"><p className="text-sm text-gray-500">This stack belongs to the active workspace and is protected by workspace RLS. Rename/archive controls can be added without changing its ownership model.</p></Section>}</div></div>}
return <div className="max-w-7xl mx-auto"><div className="flex items-start justify-between gap-4"><div><h1 className="text-3xl font-bold">Stacks</h1><p className="text-gray-500 mt-2 max-w-2xl">Group the projects and environments that make up one deployable application or service. LytHouse rolls their validation and delivery evidence into one operational view.</p></div><button className="btn-primary" onClick={()=>setCreating(true)}><Plus size={16}/> New stack</button></div>{error&&<Err>{error}</Err>}<div className="mt-7">{stacks.length?<div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{stacks.map(s=>{const n=projects.filter(p=>p.stack_id===s.id).length;return <button key={s.id} onClick={()=>{setSelected(s);setTab('Overview')}} className="text-left rounded-2xl border bg-white p-5 hover:border-violet-300 hover:shadow-sm"><div className="flex gap-3"><span className="w-10 h-10 rounded-xl bg-violet-50 text-violet-600 grid place-items-center"><Layers size={20}/></span><div><h3 className="font-semibold">{s.name}</h3><p className="text-xs text-gray-500">{n} project{n===1?'':'s'}</p></div></div><p className="text-sm text-gray-500 mt-4">{s.description||'Application system'}</p></button>})}</div>:<EmptyState icon={<Layers size={23}/>} title="No stacks yet" description="Create a stack for an application or service, then group its repositories and environments together." action={<button className="btn-primary" onClick={()=>setCreating(true)}><Plus size={16}/> Create stack</button>}/>}</div>{creating&&<div className="fixed inset-0 z-50 bg-black/40 flex items-center justify-center p-4" onClick={()=>setCreating(false)}><div className="bg-white rounded-2xl w-full max-w-lg p-6 shadow-xl" onClick={e=>e.stopPropagation()}><div className="flex justify-between"><div><h2 className="text-xl font-bold">Create stack</h2><p className="text-sm text-gray-500 mt-1">Represent one deployable application or service.</p></div><button onClick={()=>setCreating(false)}><X size={20}/></button></div><label className="label mt-5">Stack name</label><input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Customer Storefront"/><label className="label mt-4">Description</label><textarea className="input min-h-24" value={description} onChange={e=>setDescription(e.target.value)} placeholder="Repositories and environments that power the customer storefront."/><div className="flex justify-end gap-2 mt-6"><button className="btn-secondary" onClick={()=>setCreating(false)}>Cancel</button><button className="btn-primary" disabled={busy||!name.trim()} onClick={create}>{busy&&<Spinner size={14}/>} Create stack</button></div></div></div>}</div>}
function Metric({icon,value,label}){return <div className="rounded-2xl border bg-white p-5 flex gap-4"><span className="w-10 h-10 rounded-xl bg-violet-50 text-violet-600 grid place-items-center">{icon}</span><div><p className="text-2xl font-bold">{value}</p><p className="text-sm text-gray-500">{label}</p></div></div>}function Section({title,children}){return <div className="mt-5 rounded-2xl border bg-white p-5"><h2 className="font-semibold text-lg mb-3">{title}</h2>{children}</div>}function Row({icon,title,sub}){return <div className="flex gap-3 py-3 border-t first:border-t-0"><span className="text-gray-400">{icon}</span><div><p className="text-sm font-medium">{title}</p><p className="text-xs text-gray-500 mt-0.5">{sub}</p></div></div>}function Err({children}){return <div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{children}</div>}
