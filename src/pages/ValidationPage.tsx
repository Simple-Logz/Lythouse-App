import { useEffect, useState, useCallback } from 'react';
import { supabase, type Validation, type ValidationStep, type Finding, type Project } from '../lib/supabase';
import { Link, useRouter } from '../lib/router';
import { RiskGauge, SeverityBadge, StatusBadge, EmptyState, Spinner, StepIcon, timeAgo, fmtDuration } from '../lib/ui';
import { ArrowLeft, GitBranch, ScanLine, Brain, Gauge, FileText, ChevronRight, ShieldAlert, AlertTriangle, ShieldCheck, Lightbulb, FileCode2, Activity, Rocket, X, RefreshCw, ExternalLink, Wrench, CircleHelp } from 'lucide-react';

const STEP_ICONS: Record<string, typeof GitBranch> = { fetch: GitBranch, scan: ScanLine, analyze: Brain, score: Gauge, report: FileText };
const DOCS:Record<string,{label:string,url:string}>={
 dependency:{label:'npm dependency troubleshooting',url:'https://docs.npmjs.com/cli/v11/using-npm/troubleshooting'},
 dependencies:{label:'npm dependency troubleshooting',url:'https://docs.npmjs.com/cli/v11/using-npm/troubleshooting'},
 security:{label:'OWASP remediation guidance',url:'https://owasp.org/www-project-top-ten/'},
 secrets:{label:'GitHub secret scanning guidance',url:'https://docs.github.com/en/code-security/secret-scanning/introduction/about-secret-scanning'},
 typescript:{label:'TypeScript documentation',url:'https://www.typescriptlang.org/docs/'},
 build:{label:'Vite troubleshooting guide',url:'https://vite.dev/guide/troubleshooting.html'},
};
function docsFor(category?:string|null){const k=String(category||'').toLowerCase();return Object.entries(DOCS).find(([key])=>k.includes(key))?.[1]||null}
function failureCause(validation:Validation,failed:ValidationStep[]){return failed[0]?.detail||validation.summary||'The validation service reported a failure before it could produce a more specific diagnostic.'}

export function ValidationPage({ validationId }: { validationId: string }) {
  const { navigate } = useRouter();
  const [validation, setValidation] = useState<Validation | null>(null),[steps,setSteps]=useState<ValidationStep[]>([]),[findings,setFindings]=useState<Finding[]>([]),[project,setProject]=useState<Project|null>(null),[loading,setLoading]=useState(true),[activeFinding,setActiveFinding]=useState<string|null>(null),[existingDeployment,setExistingDeployment]=useState<string|null>(null),[showDeployModal,setShowDeployModal]=useState(false),[rerunning,setRerunning]=useState(false),[rerunError,setRerunError]=useState('');
  const load=useCallback(async()=>{const{data:v}=await supabase.from('validations').select('*').eq('id',validationId).maybeSingle();if(!v){setLoading(false);return}const val=v as Validation;setValidation(val);const[{data:s},{data:f},{data:proj},{data:dep}]=await Promise.all([supabase.from('validation_steps').select('*').eq('validation_id',validationId).order('step_index',{ascending:true}),supabase.from('findings').select('*').eq('validation_id',validationId).order('created_at',{ascending:true}),supabase.from('projects').select('*').eq('id',val.project_id).maybeSingle(),supabase.from('deployments').select('id').eq('validation_id',validationId).maybeSingle()]);setSteps((s??[])as ValidationStep[]);setFindings((f??[])as Finding[]);setProject(proj as Project|null);setExistingDeployment(dep?.id??null);setLoading(false)},[validationId]);
  useEffect(()=>{load()},[load]);useEffect(()=>{if(!validation||!['running','pending'].includes(validation.status))return;const interval=setInterval(load,1500);return()=>clearInterval(interval)},[validation?.status,load]);
  async function rerun(){if(!validation)return;setRerunning(true);setRerunError('');try{const{data,error}=await supabase.from('validations').insert({project_id:validation.project_id,workspace_id:validation.workspace_id,status:'pending',trigger:'manual',commit_sha:validation.commit_sha,total_findings:0,critical_count:0,high_count:0,medium_count:0,low_count:0}).select('id').single();if(error)throw error;navigate(`/validations/${data.id}`)}catch(e:any){setRerunError(e?.message||'Could not start another validation.')}finally{setRerunning(false)}}
  if(loading)return <div className="flex items-center justify-center py-24 text-gray-400"><Spinner size={22}/></div>;
  if(!validation)return <div className="card"><EmptyState icon={<AlertTriangle size={22}/>} title="Validation not found" description="This validation may have been deleted or you don't have access." action={<Link to="/projects" className="btn-primary">Back to projects</Link>}/></div>;
  const isRunning=['running','pending'].includes(validation.status),failedSteps=steps.filter(s=>s.status==='failed'),isFailed=validation.status==='failed',sevOrder={critical:4,high:3,medium:2,low:1},sortedFindings=[...findings].sort((a,b)=>(sevOrder[b.severity as keyof typeof sevOrder]??0)-(sevOrder[a.severity as keyof typeof sevOrder]??0));
  return <><Link to={project?`/projects/${project.id}`:'/projects'} className="mb-4 inline-flex items-center gap-1.5 text-sm text-gray-500 transition-colors hover:text-navy-900"><ArrowLeft size={14}/>{project?.name??'Projects'}</Link>
  <div className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-3"><h1 className="text-2xl font-bold tracking-tight text-navy-900">{validation.commit_sha?<span className="font-mono">{validation.commit_sha.slice(0,7)}</span>:'Validation'}</h1><StatusBadge status={validation.status}/></div><p className="mt-1 text-sm text-gray-500">{project?.name??'Unknown'} · {timeAgo(validation.created_at)} · {fmtDuration(validation.duration_ms)}</p></div><div className="flex items-center gap-4">{validation.risk_score!==null&&<><RiskGauge score={validation.risk_score} size={88}/>{validation.severity&&<SeverityBadge severity={validation.severity}/>}</>}{validation.status==='completed'&&(existingDeployment?<Link to={`/deployments/${existingDeployment}`} className="btn-secondary"><Rocket size={15}/> View deployment</Link>:<button onClick={()=>setShowDeployModal(true)} className="btn-primary"><Rocket size={15}/> Request deployment</button>)}</div></div>
  {isFailed&&<FailurePanel validation={validation} failedSteps={failedSteps} findings={sortedFindings} rerunning={rerunning} rerunError={rerunError} onRerun={rerun}/>} 
  {validation.summary&&!isFailed&&<div className="card mb-6 p-6 animate-fade-in"><div className="mb-2 flex items-center gap-2"><FileText size={14} className="text-brand-600"/><h3 className="text-sm font-bold text-navy-900">AI summary</h3></div><p className="text-sm leading-relaxed text-gray-600">{validation.summary}</p></div>}
  <div className="grid grid-cols-1 gap-6 lg:grid-cols-5"><div className="lg:col-span-2"><div className="card p-6"><h3 className="mb-6 text-sm font-bold text-navy-900">Pipeline</h3><div className="relative">{steps.map((step,i)=>{const Icon=STEP_ICONS[step.key]??Activity,isLast=i===steps.length-1,connectorColor=step.status==='completed'?'bg-brand-500':step.status==='failed'?'bg-red-400':'bg-gray-200';return <div key={step.id} className={`relative flex gap-3.5 pb-7 last:pb-0 ${step.status==='failed'?'rounded-xl bg-red-50/70 p-3 -mx-3 mb-3':''}`}>{!isLast&&<div className={`absolute left-[15px] top-9 h-[calc(100%-2.25rem)] w-0.5 ${connectorColor}`}/>}<StepIcon status={step.status} icon={<Icon size={14}/>}/><div className="min-w-0 flex-1 pt-1"><div className="flex items-center justify-between gap-2"><p className={`text-sm font-semibold ${step.status==='pending'?'text-gray-400':step.status==='failed'?'text-red-800':'text-navy-900'}`}>{step.name}</p>{step.duration_ms!==null&&<span className="text-2xs text-gray-400 tabular-nums">{fmtDuration(step.duration_ms)}</span>}</div>{step.detail&&<p className={`mt-1 text-sm leading-relaxed ${step.status==='failed'?'text-red-700':'text-gray-500'}`}>{step.detail}</p>}{step.status==='running'&&<p className="mt-1 text-xs text-brand-600">Running…</p>}{step.status==='failed'&&<p className="mt-2 text-xs font-semibold text-red-700">This step caused or contributed to the failed validation.</p>}</div></div>})}{steps.length===0&&isRunning&&<div className="flex items-center gap-2 text-sm text-gray-400"><Spinner size={15}/> Initializing pipeline…</div>}</div></div></div>
  <div className="lg:col-span-3"><div className="card overflow-hidden"><div className="flex items-center justify-between border-b border-gray-100 px-5 py-4"><h3 className="text-sm font-bold text-navy-900">Findings <span className="ml-1 text-gray-400">({findings.length})</span></h3>{isRunning&&<span className="flex items-center gap-1.5 text-xs text-brand-600"><Spinner size={11}/> Analyzing…</span>}</div>{findings.length===0&&!isRunning&&<EmptyState icon={isFailed?<AlertTriangle size={22}/>:<ShieldCheck size={22}/>} title={isFailed?'No structured findings were produced':'No findings'} description={isFailed?'The validation stopped before structured findings were produced. Review the failure diagnostic and failed pipeline step on this page.':'This validation completed with no issues detected. The deployment looks clean.'}/>} {findings.length===0&&isRunning&&<div className="flex flex-col items-center justify-center py-20 text-gray-400"><Spinner size={22}/><p className="mt-3 text-sm">Waiting for analysis results…</p></div>}{findings.length>0&&<div className="divide-y divide-gray-100">{sortedFindings.map(f=><FindingRow key={f.id} finding={f} expanded={activeFinding===f.id} onToggle={()=>setActiveFinding(activeFinding===f.id?null:f.id)}/>)}</div>}</div></div></div>
  {showDeployModal&&validation&&project&&<DeployModal validation={validation} projectName={project.name} onClose={()=>setShowDeployModal(false)} onCreated={depId=>{setShowDeployModal(false);navigate(`/deployments/${depId}`)}}/>}</>;
}

function FailurePanel({validation,failedSteps,findings,rerunning,rerunError,onRerun}:{validation:Validation;failedSteps:ValidationStep[];findings:Finding[];rerunning:boolean;rerunError:string;onRerun:()=>void}){const cause=failureCause(validation,failedSteps),top=findings[0],doc=docsFor(top?.category||failedSteps[0]?.key),fix=top?.recommendation||'Resolve the error shown above, then run the validation again against the same commit. If the failure is environmental, confirm repository access, required configuration and build/runtime dependencies before retrying.';return <div className="mb-6 overflow-hidden rounded-2xl border border-red-200 bg-white shadow-sm"><div className="flex flex-col gap-4 border-b border-red-100 bg-red-50 px-6 py-5 sm:flex-row sm:items-start sm:justify-between"><div className="flex gap-3"><div className="mt-0.5 rounded-lg bg-red-100 p-2 text-red-700"><ShieldAlert size={20}/></div><div><p className="text-xs font-bold uppercase tracking-wider text-red-600">Validation failed</p><h2 className="mt-1 text-lg font-bold text-red-950">Action required before this change is ready</h2><p className="mt-1 max-w-3xl text-sm leading-relaxed text-red-800">LytHouse has preserved the diagnostic evidence below instead of returning an unexplained failure status.</p></div></div><button onClick={onRerun} disabled={rerunning} className="btn-secondary shrink-0 border-red-200 text-red-700 hover:bg-red-100">{rerunning?<Spinner size={14}/>:<RefreshCw size={14}/>} Run again</button></div><div className="grid gap-0 lg:grid-cols-2"><div className="border-b border-gray-100 p-6 lg:border-b-0 lg:border-r"><div className="flex items-center gap-2 text-sm font-bold text-navy-900"><CircleHelp size={16} className="text-red-600"/> Why it failed</div><p className="mt-3 text-sm leading-relaxed text-gray-700">{cause}</p>{failedSteps[0]&&<div className="mt-4 rounded-xl bg-slate-50 p-3"><p className="text-[11px] font-bold uppercase tracking-wide text-gray-400">Failed pipeline step</p><p className="mt-1 text-sm font-semibold text-navy-900">{failedSteps[0].name}</p>{failedSteps[0].detail&&failedSteps[0].detail!==cause&&<p className="mt-1 text-xs leading-relaxed text-gray-600">{failedSteps[0].detail}</p>}</div>}</div><div className="p-6"><div className="flex items-center gap-2 text-sm font-bold text-navy-900"><Wrench size={16} className="text-brand-600"/> How to resolve it</div><p className="mt-3 text-sm leading-relaxed text-gray-700">{fix}</p><div className="mt-4 flex flex-wrap gap-2">{doc&&<a href={doc.url} target="_blank" rel="noreferrer" className="btn-secondary text-xs">{doc.label}<ExternalLink size={13}/></a>}<a href="https://docs.github.com/en/actions/monitoring-and-troubleshooting-workflows/troubleshooting-workflows" target="_blank" rel="noreferrer" className="btn-secondary text-xs">Troubleshooting reference<ExternalLink size={13}/></a></div></div></div>{rerunError&&<div className="border-t border-red-100 bg-red-50 px-6 py-3 text-sm text-red-700">{rerunError}</div>}</div>}

function DeployModal({validation,projectName,onClose,onCreated}:{validation:Validation;projectName:string;onClose:()=>void;onCreated:(id:string)=>void}){const[environment,setEnvironment]=useState<'staging'|'production'|'preview'>('staging'),[busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null),gatePassed=(validation.risk_score??0)<50&&validation.critical_count===0;async function submit(){setBusy(true);setError(null);const{data,error:insErr}=await supabase.from('deployments').insert({validation_id:validation.id,project_id:validation.project_id,workspace_id:validation.workspace_id,environment,gate_passed:gatePassed}).select('id').single();setBusy(false);if(insErr){setError(insErr.message);return}onCreated(data.id)}return <div className="fixed inset-0 z-50 flex items-center justify-center bg-navy-900/40 backdrop-blur-sm" onClick={onClose}><div className="card w-full max-w-md p-7 shadow-pop" onClick={e=>e.stopPropagation()}><div className="mb-5 flex items-center justify-between"><div><h2 className="text-lg font-bold text-navy-900">Request deployment</h2><p className="mt-0.5 text-sm text-gray-500">{projectName} · <span className="font-mono">{validation.commit_sha?.slice(0,7)??'—'}</span></p></div><button onClick={onClose} className="btn-ghost p-1.5"><X size={17}/></button></div><div className={`mb-5 flex items-center gap-3 rounded-xl border p-4 ${gatePassed?'border-brand-200 bg-brand-50/60':'border-red-200 bg-red-50/60'}`}>{gatePassed?<ShieldCheck size={20} className="text-brand-600"/>:<ShieldAlert size={20} className="text-danger-600"/>}<div><p className={`text-sm font-semibold ${gatePassed?'text-brand-700':'text-danger-600'}`}>{gatePassed?'Gate passed':'Gate blocked'}</p><p className="text-xs text-gray-500">Risk score: {validation.risk_score??'—'} · Critical: {validation.critical_count}</p></div></div><label className="label">Target environment</label><div className="mb-5 grid grid-cols-3 gap-2">{(['preview','staging','production']as const).map(env=><button key={env} onClick={()=>setEnvironment(env)} className={`rounded-xl border px-3 py-2.5 text-sm font-medium capitalize ${environment===env?'border-brand-300 bg-brand-50 text-brand-700':'border-[#d4d4d8] bg-white text-navy-600'}`}>{env}</button>)}</div>{error&&<p className="mb-4 text-sm text-danger-600">{error}</p>}<div className="flex justify-end gap-2"><button onClick={onClose} className="btn-secondary">Cancel</button><button onClick={submit} className="btn-primary" disabled={busy}>{busy?<Spinner size={15}/>:<Rocket size={15}/>} Submit request</button></div></div></div>}

function FindingRow({finding,expanded,onToggle}:{finding:Finding;expanded:boolean;onToggle:()=>void}){const sevIcon=finding.severity==='critical'?<ShieldAlert size={14}/>:finding.severity==='high'?<ShieldAlert size={14}/>:finding.severity==='medium'?<AlertTriangle size={14}/>:<ShieldCheck size={14}/>,sevText=finding.severity==='critical'?'text-danger-600':finding.severity==='high'?'text-amber-600':finding.severity==='medium'?'text-blue-600':'text-gray-500',doc=docsFor(finding.category);return <div className="transition-colors hover:bg-gray-50/60"><button onClick={onToggle} className="flex w-full items-start gap-3 px-5 py-4 text-left"><span className={`mt-0.5 shrink-0 ${sevText}`}>{sevIcon}</span><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-navy-900">{finding.title}</p><div className="mt-2 flex flex-wrap items-center gap-1.5 text-2xs text-gray-500"><SeverityBadge severity={finding.severity}/><span className="chip border border-[#d4d4d8] bg-gray-50 text-navy-600">{finding.category.replace('_',' ')}</span>{finding.file_path&&<span className="flex items-center gap-1 font-mono text-gray-400"><FileCode2 size={10}/>{finding.file_path}{finding.line?`:${finding.line}`:''}</span>}{finding.confidence!==null&&<span className="text-gray-400">conf {finding.confidence}%</span>}</div></div><ChevronRight size={15} className={`mt-1 shrink-0 text-gray-300 transition-transform ${expanded?'rotate-90':''}`}/></button>{expanded&&<div className="animate-fade-in px-5 pb-5 pl-[48px]"><div className="grid gap-3"><div><p className="text-2xs font-bold uppercase tracking-wide text-gray-400">Problem</p><p className="mt-1 text-sm leading-relaxed text-gray-700">{finding.description}</p></div><div className="rounded-xl border border-amber-200 bg-amber-50/60 p-3.5"><p className="text-2xs font-bold uppercase tracking-wide text-amber-700">Why it matters</p><p className="mt-1 text-sm leading-relaxed text-amber-900">A {finding.severity} validation finding can make this release unsafe or unreliable. Resolve it before treating the validation as deployment-ready.</p></div>{finding.recommendation&&<div className="flex gap-2.5 rounded-xl border border-brand-200 bg-brand-50/60 p-3.5"><Lightbulb size={15} className="mt-0.5 shrink-0 text-brand-600"/><div><p className="text-2xs font-bold uppercase tracking-wide text-brand-700">Recommended fix</p><p className="mt-1 text-sm leading-relaxed text-navy-700">{finding.recommendation}</p></div></div>}<div className="rounded-xl border border-gray-200 bg-gray-50 p-3.5"><p className="text-2xs font-bold uppercase tracking-wide text-gray-500">Verify fix</p><p className="mt-1 text-sm leading-relaxed text-gray-700">Apply the remediation, commit the change, then run validation again. The finding should no longer be open and the failed pipeline step should complete successfully.</p>{doc&&<a href={doc.url} target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-1 text-xs font-semibold text-brand-700 hover:underline">Open relevant documentation <ExternalLink size={12}/></a>}</div></div></div>}</div>}
