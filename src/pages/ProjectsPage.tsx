// @ts-nocheck
import{useEffect,useState}from'react';
import{supabase,type Project}from'../lib/supabase';
import{PageHeader,EmptyState,Spinner}from'../lib/ui';
import{useRouter,Link}from'../lib/router';
import{FolderGit2,Plus,GitFork as Github,Search,ChevronRight,Lock,X}from'lucide-react';
import{usePlanId}from'./AppShell';import{PLAN_LIMITS}from'../lib/planLimits';
const hostFrom=(u:string)=>{try{return new URL(u).hostname.replace(/^www\./,'')}catch{return u}};
export function ProjectsPage(){const{navigate}=useRouter();const planId=usePlanId();const[loading,setLoading]=useState(true),[projects,setProjects]=useState<Project[]>([]),[workspaces,setWorkspaces]=useState<any[]>([]),[wid,setWid]=useState(localStorage.getItem('sandbox.activeWs')||''),[q,setQ]=useState(''),[modal,setModal]=useState(false),[name,setName]=useState(''),[url,setUrl]=useState(''),[branch,setBranch]=useState('main'),[saving,setSaving]=useState(false),[error,setError]=useState('');const limit=PLAN_LIMITS[planId]?.projects??null,atLimit=limit!=null&&projects.length>=limit;
async function load(){setLoading(true);setError('');const{data:ws,error:we}=await supabase.from('workspaces').select('id,name').order('created_at',{ascending:true});if(we){setError(we.message);setLoading(false);return}const list=ws??[];setWorkspaces(list);let active=wid;if(!active||!list.some(w=>w.id===active)){active=list[0]?.id||'';if(active){localStorage.setItem('sandbox.activeWs',active);setWid(active);window.dispatchEvent(new Event('lythouse:workspace-changed'))}}if(!active){setProjects([]);setLoading(false);return}const{data,error:e}=await supabase.from('projects').select('id,name,description,status,git_url,git_branch,language,workspace_id,created_at').eq('workspace_id',active).order('created_at',{ascending:false});if(e)setError(e.message);setProjects(data??[]);setLoading(false)}
useEffect(()=>{load()},[]);useEffect(()=>{if(wid)load()},[wid]);
async function create(){if(!wid||!url.trim())return;setSaving(true);setError('');const final=(name.trim()||url.replace(/\.git$/,'').split('/').filter(Boolean).pop()||'Project');const{data,error:e}=await supabase.from('projects').insert({workspace_id:wid,name:final,git_url:url.trim(),git_branch:branch.trim()||'main',status:'active'}).select('id,name,description,status,git_url,git_branch,language,workspace_id,created_at').single();if(e)setError(e.message);else{setProjects(p=>[data,...p]);setModal(false);setName('');setUrl('');setBranch('main');navigate('/projects/'+data.id)}setSaving(false)}
if(loading)return <div className="flex justify-center py-24"><Spinner size={28}/></div>;const filtered=projects.filter(p=>!q||p.name.toLowerCase().includes(q.toLowerCase()));const canCreate=!!wid;
return <div><PageHeader title="Projects" description="Every repository connected to LytHouse for pre-deployment validation." actions={<div className="flex items-center gap-2">{workspaces.length>1&&<select className="input w-44" value={wid} onChange={e=>{localStorage.setItem('sandbox.activeWs',e.target.value);setWid(e.target.value);window.dispatchEvent(new Event('lythouse:workspace-changed'))}}>{workspaces.map(w=><option key={w.id} value={w.id}>{w.name}</option>)}</select>}{canCreate&&(atLimit?<Link to="/plans" className="btn-primary"><Lock size={14}/> Upgrade for more projects</Link>:<button onClick={()=>setModal(true)} className="btn-primary"><Plus size={16}/> Connect project</button>)}</div>}/>{error&&<div className="mb-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}{projects.length===0?<EmptyState icon={<FolderGit2 size={22}/>} title="No projects yet" description={wid?'Connect a repository to start running pre-deployment validations.':'No workspace is available for this account.'} action={canCreate&&!atLimit?<button onClick={()=>setModal(true)} className="btn-primary"><Github size={16}/> Connect repository</button>:undefined}/>:<><div className="mb-4 relative max-w-xs"><Search size={15} className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400"/><input className="input pl-9" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search projects…"/></div><div className="card overflow-hidden p-0 divide-y divide-gray-100">{filtered.map(p=><Link key={p.id} to={'/projects/'+p.id} className="flex items-center gap-4 px-4 py-4 hover:bg-gray-50"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-brand-50 text-brand-600"><FolderGit2 size={18}/></div><div className="min-w-0 flex-1"><div className="font-semibold text-navy-900">{p.name}</div><div className="text-xs text-gray-500">{p.git_url?hostFrom(p.git_url):'Repository not connected'}{p.git_branch?' · '+p.git_branch:''}</div></div><span className="chip border border-brand-200 bg-brand-50 text-brand-700">{p.status}</span><ChevronRight size={16} className="text-gray-300"/></Link>)}</div></>}{modal&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4" onClick={()=>setModal(false)}><div className="w-full max-w-lg rounded-xl bg-white p-5 shadow-xl" onClick={e=>e.stopPropagation()}><div className="mb-4 flex items-center justify-between"><div><h2 className="text-lg font-semibold">Connect repository</h2><p className="text-sm text-gray-500">Add a GitHub repository to this workspace.</p></div><button className="btn-ghost" onClick={()=>setModal(false)}><X size={16}/></button></div><label className="label">Repository URL</label><input className="input mb-3" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://github.com/org/repo"/><label className="label">Project name <span className="text-gray-400">(optional)</span></label><input className="input mb-3" value={name} onChange={e=>setName(e.target.value)} placeholder="Defaults to repository name"/><label className="label">Branch</label><input className="input mb-4" value={branch} onChange={e=>setBranch(e.target.value)}/><div className="flex justify-end gap-2"><button className="btn-secondary" onClick={()=>setModal(false)}>Cancel</button><button className="btn-primary" disabled={saving||!url.trim()} onClick={create}>{saving?'Connecting…':'Connect project'}</button></div></div></div>}</div>}
