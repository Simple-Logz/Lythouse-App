logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# NOTE: .env is intentionally still tracked for now because the Vercel build
# reads it. Once the VITE_* vars are set in the Vercel dashboard, add `.env`
# back here and run `git rm --cached .env` to stop committing it.
.env.local
.env.*.local

# Added by Lythouse — do not commit secrets
.env
.env.*
*.env
